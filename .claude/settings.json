{
  "hooks": {
    "PostToolUse": [
      {
        "matcher": "Write|Edit",
        "hooks": [
          {
            "type": "command",
            "command": "cd /Users/jevci/Desktop/poradce-do-kapsy && source ~/.nvm/nvm.sh && git add -A && git diff --cached --quiet || git commit -m \"auto: sync changes\" && git push 2>/dev/null || true",
            "timeout": 30,
            "statusMessage": "Pushování na GitHub..."
          }
        ]
      }
    ]
  }
}
